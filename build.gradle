apply plugin: 'java'

ext {
    libraries = [

            rxjava                    : "2.2.8",

            gson                      : "2.8.5",

            bytekit                   : "1.2.6",

            spring                    : "5.1.0.RELEASE"
    ]
}

group 'com.safframework.rxcache'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    jcenter()
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

buildscript {

    repositories {
        jcenter()
        mavenCentral()
    }
    dependencies {
        classpath 'com.novoda:bintray-release:0.5.0'
    }
}

tasks.getByPath(":caffeine:bintrayUpload").enabled = false
tasks.getByPath(":core:bintrayUpload").enabled = false
tasks.getByPath(":guava-cache:bintrayUpload").enabled = false
tasks.getByPath(":off-heap:bintrayUpload").enabled = false
tasks.getByPath(":proxy:bintrayUpload").enabled = false

tasks.getByPath(":converter:fastjson:bintrayUpload").enabled = true
tasks.getByPath(":converter:moshi:bintrayUpload").enabled = true